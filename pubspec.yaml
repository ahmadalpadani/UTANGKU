name: utangku_app
description: "Aplikasi pencatatan utang piutang dengan WhatsApp integration."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Database & Storage
  sqflite: ^2.3.0
  path: ^1.8.3

  # State Management
  provider: ^6.1.1

  # UI Components
  cupertino_icons: ^1.0.6
  fl_chart: ^0.66.0

  # Date & Time
  intl: ^0.20.2
  table_calendar: ^3.0.9

  # WhatsApp & Contacts
  url_launcher: ^6.2.1
  whatsapp_unilink: ^2.1.0

  # Utilities
  currency_formatter: ^2.2.1

  # Notifications
  flutter_local_notifications: ^18.0.1
  timezone: ^0.10.0
  flutter_timezone: ^3.0.1

  # Storage
  shared_preferences: ^2.3.5

  # Security
  local_auth: ^2.3.0
  flutter_secure_storage: ^9.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
